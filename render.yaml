services:
  - type: web
    name: chatbot-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_OPTIONS
        value: --max-old-space-size=2048
